# Generated Artifacts

Reference outputs, fitted parameters, simulated catalogs, forecasts, logs, and
evaluation products are generated here and are not committed. Every retained
artifact set must have a committed manifest with checksums and the producing
Git commit.

